spring.application.name=extractor

spring.datasource.url=jdbc:postgresql://postgres:5432/carolaiextractor
spring.datasource.username=carolai
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:carolai}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.default_batch_fetch_size=100

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

extractor.login.email=${EXTRACTOR_LOGIN_EMAIL:carolsilva27personal@gmail.com}
extractor.login.password=${EXTRACTOR_LOGIN_PASSWORD:}

firestore.project-id=personalfit-55234
firestore.base-url=https://firestore.googleapis.com/v1/projects/${firestore.project-id}/databases/(default)/documents
firestore.collection-treinos=TreinosLivres
firestore.personal-id=sSvz8jlKe4YEjs03YhQxR5GUP3g2

# External API — Physical Assessment (PersonalFit Cloud Function)
external-api.physical-assessment-base-url=${PHYSICAL_ASSESSMENT_BASE_URL:https://southamerica-east1-personalfit-55234.cloudfunctions.net}
external-api.physical-assessment-endpoint=${PHYSICAL_ASSESSMENT_ENDPOINT:/physicalAssessment/searchPhysicalAssessment}
external-api.physical-assessment-token=${PHYSICAL_ASSESSMENT_TOKEN:functionAlunoAntigo}

# Platform API (migration target)
carolai.platform.url=${PLATFORM_URL:http://localhost:8081}
carolai.platform.email=${PLATFORM_EMAIL:admin@example.com}
carolai.platform.password=${PLATFORM_PASSWORD:Admin123!}
carolai.platform.tenant-id=${PLATFORM_TENANT_ID:}
